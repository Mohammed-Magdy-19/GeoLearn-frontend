/**
 * src/features/auth/components/authFormStyles.ts
 *
 * @deprecated — This file is no longer used. Auth forms now use shadcn/ui
 * components (Input, Label, Button) directly. Kept temporarily for reference.
 *
 * Previously contained INPUT_CLASS and SUBMIT_BUTTON_CLASS constants
 * shared across LoginForm and RegisterForm.
 */

// No exports — all auth forms now use shadcn components directly.